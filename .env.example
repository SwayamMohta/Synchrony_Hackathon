# PostgreSQL / pgvector
DATABASE_URL=postgresql+psycopg2://uw:uw@localhost:5432/underwriting
POSTGRES_USER=uw
POSTGRES_PASSWORD=uw
POSTGRES_DB=underwriting

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_MIN=120

# CORS
CORS_ORIGIN=http://localhost:5173

# OpenAI (RAG assistant - optional; leave blank to use the offline FakeLLM fallback)
OPENAI_API_KEY=
OPENAI_MODEL_ID=gpt-4o-mini

# Gemini (RAG assistant - set either GEMINI_API_KEY or OPENAI_API_KEY; Gemini wins)
GEMINI_API_KEY=
GEMINI_MODEL_ID=gemini-3.6-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# RAG embedding model + policy corpus directory
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
POLICIES_DIR=policies

# Feature metadata path (optional override)
FEATURE_METADATA=ml/feature_metadata.json
